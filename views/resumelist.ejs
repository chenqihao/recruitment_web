<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <meta http-equiv="Pragma" content="no-cache">
    <meta http-equiv="Cache-Control" content="no-cache">
    <meta http-equiv="Expires" content="0">
    <title><%= title %></title>
    <link href="/stylesheets/index.css" type="text/css" rel="stylesheet">
    <script type="text/javascript" src="../javascripts/jquery.js"></script>
    <script type="text/javascript" src="../javascripts/jquery_index.js"></script>
    <script type="text/javascript" src="../javascripts/jquery_resumelist.js"></script>
  </head>
  <body>
    <div class="header">
      <div class="big_title"><%= title %></div>
      <div class="user_area">
        <div class="username_area"><%= userdata.username %></div>
        <input type="hidden" class="hidden_username" value="<%= userdata.username %>" disabled="disabled">
        <input type="hidden" class="hidden_usertype" value="<%= userdata.usertype %>" disabled="disabled">
        <div class="toggle_box" style="display:none;">
          <div class="modinfo_area admin_need_hide">修改信息</div>
          <div class="chgpwd_area admin_need_hide">修改密码</div>
          <div class="logout_area">退出登录</div>
        </div>
      </div>
    </div>
    <div class="nav_bar">
      <div class="nav_bar_blank"></div>
      <div class="nav_bar_select redirect_resume_button" style="border:0; border-bottom:2px solid white; color:white;">简历管理</div>
      <div class="nav_bar_select redirect_jobsearch_button">职位搜索</div>
      <div class="nav_bar_select redirect_apprecord_button">申请记录</div>
    </div>
    <div class="body">
      <div class="resumelist_box">
        <ul class="list_title">
          <li class="list_title_column">简历名称</li>
          <li class="list_title_column">是否默认</li>
          <li class="list_title_column">是否公开</li>
          <li class="list_title_column">简历操作</li>
        </ul>
        <% for (var i in resumeList){ %>
        <ul class="list_row">
          <input type="hidden" class="hidden_resumeId" value="<%= resumeList[i]._id %>" disabled="disabled">
          <li class="list_column"><a  href="/person/myresume?_id=<%= resumeList[i]._id %>"><%= resumeList[i].resumename %></a></li>
          <li class="list_column">
            <% if(resumeList[i].isDefault){ %>
            <button class="select_button default_select" style="background:rgb(0,188,212)">是</button>
            <% }else { %>
            <button class="select_button default_select" style="background:#BBBBBB">否</button>
            <% } %>
          </li>
          <li class="list_column">
            <% if(resumeList[i].isPublic){ %>
            <button class="select_button public_select" style="background:rgb(0,188,212)">是</button>
            <% }else { %>
            <button class="select_button public_select" style="background:#BBBBBB">否</button>
            <% } %>
          </li>
          <li class="list_column"><a class="opt_click remove_resume_btn">删除</a></li>
        </ul>
        <% } %>
        <button class="list_btn" id="create_resume_button">新建
        </button>
      </div>
      <div class="page_box">
        <div class="pre_page_btn">
          &nbsp;
          <% if (page > 1){ %>
          <a href="?page=<%= parseInt(page)-1 %>">上一页</a>
          <% } %>
        </div>
        <div class="page_text">
          <a "><%= page %></a>
        </div>
        <div class="next_page_btn">
          <% if (page < maxpage){ %>
          <a href="?page=<%= parseInt(page)+1 %>">下一页</a>
          <% } %>
          &nbsp;
        </div>
      </div>
    </div>
    <div class="footer">developed by cqh&nbsp;&nbsp;&nbsp;&nbsp;</div>
  </body>
</html>
